# Environment configuration template for F1 Analytics
# Copy this file to .env and adjust the values.

# OpenF1 API does not require an API key, but the setting is ready for future extension.
OPENF1_BASE_URL=https://api.openf1.org/v1
